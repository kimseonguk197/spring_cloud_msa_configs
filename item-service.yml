server:
  port: 8081
spring:
  profiles: item
  datasource:
    url: jdbc:mariadb://localhost:3306/item
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
